<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.p3.printedpost.CommentActivity">
    <item android:id="@+id/order_by"
        android:title="@string/order_by"
        android:icon="@drawable/sort_by"
        android:orderInCategory="100"
        app:showAsAction="never">
        <menu>
            <item android:id="@+id/by_oldest"
                android:title="@string/by_oldest"
                />
            <item android:id="@+id/by_newest"
                android:title="@string/by_newest" />

            <item android:id="@+id/by_coolest"
                android:title="@string/by_coolest" />
        </menu>
    </item>

</menu>
